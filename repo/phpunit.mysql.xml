<?xml version="1.0" encoding="UTF-8"?>
<!--
  MySQL-backed integration test configuration.
  Runs against a real MySQL instance to verify production-specific guarantees:
  - Row-level locking (SELECT ... FOR UPDATE)
  - Idempotency key composite uniqueness
  - DateTime column precision
  - Transaction rollback behavior

  Prerequisites:
    MySQL running on 127.0.0.1:3306 with database 'campus_platform_test'
    User: campus / campus_secret (or override via env vars)

  Usage:
    docker compose exec app vendor/bin/phpunit -c phpunit.mysql.xml
    # Or locally:
    DB_HOST=127.0.0.1 DB_DATABASE=campus_platform_test vendor/bin/phpunit -c phpunit.mysql.xml
-->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!-- force="true" required because the container's /var/www/.env
             sets DB_DATABASE=campus_platform (production); without force, the
             already-set env var wins and the suite lands on the prod DB. -->
        <env name="APP_ENV" value="testing" force="true"/>
        <env name="APP_KEY" value="base64:YWFhYWFhYWFhYWFhYWFhYWFhYWFhYWFhYWFhYWFhYWE=" force="true"/>
        <env name="BCRYPT_ROUNDS" value="4" force="true"/>
        <env name="CACHE_STORE" value="array" force="true"/>
        <env name="DB_CONNECTION" value="mysql" force="true"/>
        <env name="DB_HOST" value="127.0.0.1" force="true"/>
        <env name="DB_PORT" value="3306" force="true"/>
        <env name="DB_DATABASE" value="campus_platform_test" force="true"/>
        <env name="DB_USERNAME" value="campus" force="true"/>
        <env name="DB_PASSWORD" value="campus_secret" force="true"/>
        <env name="QUEUE_CONNECTION" value="sync" force="true"/>
        <env name="SESSION_DRIVER" value="array" force="true"/>
    </php>
</phpunit>
